# ---- Secrets & config ----
.env
*.env
!.env.example
secrets/

# ---- Data & generated output ----
# (git has NO trailing/inline comments on patterns — keep each comment on its OWN line or the rule breaks)
# repo-root runtime data (DB, dev cache) + the operator's LOCAL curated catalog; packaged sounds live under src/
/data/*
!/data/affiliate_catalog.example.json
output/
*.db
*.sqlite3
# derived transparent avatar cutouts (regenerated from the source avatar)
*.cutout.png

# ---- Python ----
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.env.bak

# ---- Tooling caches ----
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# ---- Editor / OS ----
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# ---- Personal / channel-specific / AI working files (never push) ----
Future_Plans.txt
Video_ideas.txt
Reference.md
Claude.md
CLAUDE.md
*.claude.md
AGENTS.md
GEMINI.md
.memory/
memories/
# the operator's own face + voice sample — every user supplies their own; never ship these
assets/avatar.png
assets/avatar.cutout.png
assets/voice_reference.wav
