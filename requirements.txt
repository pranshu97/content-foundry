anthropic>=0.39
openai>=1.50
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.2
ddgs>=6.0              # free web search data source (DuckDuckGo, no key)
SQLAlchemy>=2.0
typer>=0.12
APScheduler>=3.10
streamlit>=1.37
tenacity>=8.5
structlog>=24.1
rich>=13.7
python-dateutil>=2.9
python-ulid>=2.7
# media / production
elevenlabs>=1.5
pydub>=0.25            # overlay SFX onto narration + decode/concat the per-scene voiceover
edge-tts>=6.1          # free online neural TTS (no API key)
piper-tts>=1.2         # free fully-offline neural TTS (needs a downloaded .onnx voice)
stability-sdk>=0.8
Pillow>=10.4
ffmpeg-python>=0.2
moviepy>=1.0.3
num2words>=0.5         # expand numbers to words for TTS ("202,000" -> "two hundred two thousand")
# Draws the shots a camera cannot take (levelling matrix, comp bands, pipeline) instead of asking an
# image model to fake one. Capped BELOW 3.10 deliberately: 3.8/3.9 are built against numpy 1.x, which
# the Chatterbox pin below requires. graphviz was evaluated and rejected — the conda-forge Windows
# build has no working textlayout plugin, so every label overflowed its node.
matplotlib>=3.8,<3.10

# ---------- local ML: free voice clone (Chatterbox) ----------
# GPU strongly recommended. INSTALL CUDA TORCH FIRST — a plain `pip install` pulls the CPU-only wheel,
# and the packages below would drag it in too. Run this BEFORE `pip install -r requirements.txt`:
#   pip uninstall -y torch torchaudio
#   pip install torch==2.6.0 torchaudio==2.6.0 --index-url https://download.pytorch.org/whl/cu124
# (torch 2.6.0 / torchaudio 2.6.0 + CUDA 12.4 are the versions this project is verified on.)
chatterbox-tts==0.1.7   # zero-shot voice cloning (MIT, safe to monetize)
# Chatterbox needs diffusers==0.29.0; a newer transformers or numpy>=2 breaks diffusers 0.29's import,
# so these stay pinned:
diffusers==0.29.0
transformers==4.44.2
numpy<2.0.0
safetensors==0.5.3
accelerate>=0.28
rembg>=2.0              # avatar background cutout for the composited-thumbnail fallback

# publishing
google-api-python-client>=2.140
google-auth-oauthlib>=1.2
google-auth-httplib2>=0.2
# dev
pytest>=8.3
pytest-mock>=3.14
pytest-cov>=5.0
respx>=0.21
ruff>=0.6
mypy>=1.11
pre-commit>=3.8
