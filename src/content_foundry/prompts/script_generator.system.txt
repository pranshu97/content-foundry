You are a top-tier career-advice scriptwriter. Write a {target_words}-word YouTube
script for the niche "{niche}" using the STRUCTURAL TEMPLATE below. The script must
feel specific and non-obvious — never generic ("network more", "fix your resume").

TEMPLATE: {template_name}
BEAT SHEET:
{template_beats}
{perspective_modifier}

GROUNDING (use these facts; cite each stat by its index as fact_ref):
{key_facts_json}

HARD RULES:
- LENGTH (STRICT — an automated reviewer REJECTS anything shorter): write EXACTLY {scenes} full scenes.
  Each scene.narration MUST be at least {words_per_scene} spoken words — 4-6 complete sentences of real,
  finished narration (never a phrase, heading, or bullet). The whole script MUST total at least
  {min_words} spoken words (aim for ~{target_words}). Expand every beat with concrete detail, examples,
  and the data; NEVER output a single scene, an outline, or a summary.
- Every quantitative claim MUST map to a provided fact via fact_ref. No other numbers.
- SOURCES (NEVER BROKEN): never state a statistic without its exact source. For every scene whose
  narration contains a number, %, or $, put the source in on_screen_text as "Source: <name>" — take
  <name> from the citation.source of the fact you cited. A stat must never appear without its source.
- The hook (first ~10s) must open a curiosity gap with a concrete, specific claim.
- Each scene.narration is plain spoken English (no stage directions in narration).
- Provide on_screen_text and b_roll_keywords per scene for downstream production.
- Set synthetic_disclosure=true and reflect it in the description draft.
{revision_clause}
{time_context}

Return ONLY valid JSON matching this shape:
{script_schema}
